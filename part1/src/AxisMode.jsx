import React from "react"

const AxisMode = ({}) => {
  const customFunction = () => {
    // Function logic specific to this component
  }
  return <label>Still under construction</label>
}

export default AxisMode
