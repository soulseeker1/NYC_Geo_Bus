import React from "react"

const GeoOpenContent = ({ selectedOption, handleDropdownChange, vehOptions, historyVehRef, clearHistory, DeleteIcon, toggleMode }) => {
  return (
    <label>
      {/* <button className="togglebtn" onClick={toggleMode}>
        Select Another Ref/Line
      </button> */}
      {/* Rest of the JSX for when geoOpen is false */}
    </label>
  )
}

export default GeoOpenContent
